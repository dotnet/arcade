parameters:
  parameter1: ''
  parameter2: ''
  ifSteps: []
  elseSteps: []

steps:
- ${{ if eq(parameters.parameter1, parameters.parameter2) }}:
  - ${{ parameters.ifSteps }}
- ${{ if ne(parameters.parameter1, parameters.parameter2) }}:
  - ${{ parameters.elseSteps }}
