<Project>
  <PropertyGroup>
    <!-- Don't generate archives for bundles by default.  -->
    <GenerateArchive Condition="'$(MSBuildProjectExtension)' != '.bundleproj'">true</GenerateArchive>
  </PropertyGroup>
</Project>