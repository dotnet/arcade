
jobs:
- job: Windows_NT
  timeoutInMinutes: 90
  pool:
    name: NetCorePublic-Pool
    queue: BuildPool.Windows.10.Amd64.VS2017.Open
  steps:
  - checkout: self
    clean: true
  # Use utility script to run script command dependent on agent OS.
  - script: echo "##vso[task.logissue type=warning]this is an warning logissue"
  - script: echo "##vso[task.logdetail id=11111111-1111-1111-1111-111111111111;type=build;name=test1;order=1]create record"
  - script: echo "##vso[task.logdetail id=11111111-1111-1111-1111-111111111112;type=build;name=test2;order=1]create record2"
  - script: echo "##vso[task.logdetail id=11111111-1111-1111-1111-111111111111;type=build;name=test3;order=1]create record3"
  - script: echo "##vso[task.logissue type=error]this is an error logissue"